{
  "name": "caridana-stack-compatibility-spike",
  "private": true,
  "version": "0.0.0",
  "description": "Throwaway, synthetic-only Cloudflare compatibility proof for CariDana.FILKOM.",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json && wrangler types --check",
    "db:migrate:local": "wrangler d1 migrations apply caridana-stack-compatibility --local",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run db:migrate:local && npm run check && npm run test"
  },
  "dependencies": {
    "hono": "4.12.32",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "@types/node": "26.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
